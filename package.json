{
  "name": "fup-bun",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "bun run src/bin/fup-check.ts",
    "reset": "bun run src/bin/fup-reset.ts",
    "test": "bun test"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "mysql2": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}